# M07 真实 Agent 会话接收——确认回执

- incident_id: `m07_154304_299-keyvol-0`
- severity: WARNING
- kind: keyvol
- detail: 关键卷 F: 可用 18.0GiB < 20.0GiB（样本注入的隔离夹具事件）
- run: `runs/m07_154304_299`（task_kind=fixture；样本源=file: 显式测试输入）

## 递交链时间线（UTC）

1. 规则确立+spool 持久化（`alerts/alerts.jsonl` `alert_delivered`）：2026-09-06T15:43:05Z
2. 通知通道接受（stdout 写入宿主持久化任务输出文件）：同秒
3. 工具呈现（ZCode `TaskOutput(block=true, timeout=15000)` 真实返回 `R17ALERT {"action":"open","severity":"WARNING","incident_id":"m07_154304_299-keyvol-0",…}`）：本会话 2026-09-06T15:43:1xZ（首次等待周期）
4. **Agent 确认（认知层）**：本会话（ZCode Agent，模型 GLM-5.3）确认收到该事件；按 R17-SUPERVISION-POLICY-v1 语义处置：WARNING = 当前任务继续、禁止新增重任务、无需控制动作。

## 语义边界（如实声明）

- 传输/工具展示层证据=本回执引用的工具结果（会话内 TaskOutput 输出）与 spool 对照一致。
- "Agent 已确认"的更强证据（模型对该事件的独立处理动作）以本回执文字为准——它是 Agent 生成的持久化记录，不由任何本地程序冒充。
- 模式 B（有界工具等待/周期接收）：15 秒周期上限内的呈现延迟属预期；不声称即时推送。

## 同场附带事件（第二次运行的保护事故端到端演示）

- 2026-09-06T16:11:27Z 全量回归（run `20260906T161104_6291_26823`）触发伪 observation_stale CRITICAL（后判定为监护自身缺陷 §2.4-6）；该 CRITICAL 同样经任务输出通道被本会话接收，且**停止在接收之前已由本地策略执行**（rc=-15 早于任何模型动作）——同时构成 M10 语义的又一次真实演示。
